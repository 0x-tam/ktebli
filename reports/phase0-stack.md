# Phase 0 — closed. The stack is up on Jarvis and every check is green.

**Date:** 2026-08-28 · **Host:** Jarvis (verified: `hostname` = Jarvis, egress works) · **Result: PASS**

Supersedes the 2026-08-27 STOP record (that host was a Claude Code container with no egress;
this one is the machine the phase was written for). History preserved in git.

## The verifications

### 1. Migrations replay clean — PASS (15 migrations, not 11)

The gauntlet's count of 11 is stale: four migrations landed after it was written
(`stranded_claims_and_alerting`, `unbounded_composer`, `delivery_gate`, `sufficiency_gate`).
All **15** replay cleanly, twice over: into the throwaway PG17 cluster
(`tests/replay/run.sh` → `REPLAY OK`) and into the live local stack (`supabase db reset`).

### 2. All eight fingerprint lines MATCH, zero exclusions — PASS

Actual lines from the throwaway replay, byte-identical to `tests/replay/expected-fingerprint.txt`,
and reproduced independently by `stack/up.sh` against the running local database:

```
columns_nonvector|e8117af74ac45fcfef1ea78972e07ceb
constraints|be67c83d021beee4ed9dcbcf5e468db1
functions|e5cf57888fab2d6b9cd28a5bf11c14a4
grants|e7e7a9931b13923d26bf9315a0610ed5
indexes|bfd2c95cb6092fd119e0d04bce9bcad9
policies|66d57dd2e060c72c312926123b48dc22
rls_flags|f3f957816f9975c7cb31d8127a58811d
triggers|ec7f46725697aa62c2d33efd33b3fa41
```

**A measurement bug was caught before it became a STOP, again.** The first replay ran on
PostgreSQL 18 (Ubuntu 26.04's default) and the `constraints` category alone mismatched
(`2b51ca47…`). PG18 newly materialises NOT NULL constraints as `pg_constraint` rows
(174 `contype='n'` rows in the replayed schema); excluding them reproduces the expected
hash exactly. Production is PG **17.6**, so PG17 was installed from pgdg and the replay
re-run there: all eight match with **zero exclusions and zero query edits**.
`expected-fingerprint.txt` was not touched. This is the third time in this project a damning
number was a measurement artefact — verify the measurement before the number.

### 3. Guard passes — PASS

- `/etc/hosts` blackholes `uocauqflcqefgdixbzpf.supabase.co` and `db.…` to 127.0.0.1
  (`# ktebli-local-stack guard` entries present).
- `curl https://uocauqflcqefgdixbzpf.supabase.co/` → HTTP 000 (socket refused).
- `stack/up.sh` verifies both before starting anything, and did.
- Meanwhile openrouter.ai → 200 and www.ukyouth.org → 200, so the block is the guard,
  not a dead network.

### 4. One manual worker tick completes locally — PASS

`supabase functions serve` (edge-runtime 1.74.3 in Docker; no local Deno needed) +
POST to `http://127.0.0.1:54321/functions/v1/worker` with `x-worker-secret` from the
local Vault:

```
{"ok":true,"processed":0,"ms":12}
```

`processed: 0` is correct — the queue is empty. The worker authenticated against the
per-environment `worker_secret` (generated by migration `20260820145930`, never a literal),
read the OpenRouter key from the local Vault, and completed its claim loop.

Two small findings, queued for phase 6 rather than fixed mid-phase:
- `stack/up.sh` tells the operator to drive the worker with `stack/tick.sh`, which does not
  exist. The tick above was made directly with curl.
- The first tick without the secret returns `forbidden` — correct fail-closed behaviour,
  confirmed rather than assumed.

### 5. Key verified — PASS

`GET /api/v1/credits` (direct HTTPS): `total_credits 45.00, total_usage 19.956` →
**balance $25.04**. Floor $3.00. The key itself appears nowhere in this report or any log.
(The key as found in `stack/.env` was three concatenated paste fragments; see RUNLOG
2026-08-28 — repaired before anything used it.)

## Environment installed this session

PostgreSQL 17 (pgdg) + 18 (distro; unused after the artefact), postgresql-client,
supabase CLI 2.116.0 (npm). Docker was already present. The default PG clusters are
stopped and disabled; the replay uses its own throwaway cluster on port 5433 and the
supabase stack its own container DB on 54322.
